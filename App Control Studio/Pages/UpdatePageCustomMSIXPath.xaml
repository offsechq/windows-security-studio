<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppControlManager.Pages.UpdatePageCustomMSIXPath"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:customUI="using:AppControlManager.CustomUIElements"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    FlowDirection="{x:Bind ViewModel.AppSettings.ApplicationGlobalFlowDirection, Mode=OneWay}">

    <ScrollViewer>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <customUI:InfoBarV2 IsOpen="{x:Bind ViewModel.AppPackageInstallerInfoBarIsOpen, Mode=TwoWay}"
                                Message="{x:Bind ViewModel.AppPackageInstallerInfoBarMessage, Mode=OneWay}"
                                Severity="{x:Bind ViewModel.AppPackageInstallerInfoBarSeverity, Mode=OneWay}"
                                IsIconVisible="True"
                                IsClosable="{x:Bind ViewModel.AppPackageInstallerInfoBarIsClosable, Mode=OneWay}"
                                Grid.Row="0"/>

            <StackPanel Grid.Row="1"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"
                        Spacing="{StaticResource SettingsCardSpacing}">

                <win:StackPanel.ChildrenTransitions>
                    <win:EntranceThemeTransition FromVerticalOffset="50" />
                    <win:RepositionThemeTransition IsStaggeringEnabled="False" />
                </win:StackPanel.ChildrenTransitions>

                <customUI:SettingsCardV3 HeaderIcon="{ui:FontIcon Glyph=&#xF83E;}"
                                         x:Uid="BrowseForAppPackagePathSettingsCard">

                    <StackPanel Spacing="12">
                        <controls:WrapPanel Orientation="Horizontal"
                                            HorizontalSpacing="10"
                                            VerticalSpacing="10">
                            <customUI:ButtonV2 Click="{x:Bind ViewModel.BrowseForCustomMSIXPathButton_Click}"
                                               x:Uid="FileBrowseButton"
                                               IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                                               Style="{StaticResource AccentButtonStyle}"
                                               ObservedData="{x:Bind ViewModel.LocalPackageFilePath, Mode=OneWay}">
                                <customUI:ButtonV2.ContentTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xEC50;" FontSize="16"/>
                                            <TextBlock Text="{x:Bind}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </customUI:ButtonV2.ContentTemplate>
                            </customUI:ButtonV2>

                            <Button x:Uid="ClearButton"
                                    Click="{x:Bind ViewModel.ClearSelectedFilePath}"
                                    IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}" />
                        </controls:WrapPanel>

                        <TextBlock x:Uid="ViewSelectedFilesTextBlock"
                                   TextWrapping="WrapWholeWords" />

                        <TextBox Text="{x:Bind ViewModel.LocalPackageFilePath, Mode=OneWay}"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 IsSpellCheckEnabled="False"
                                 MinWidth="400"
                                 MaxWidth="900"
                                 IsReadOnly="True" />
                    </StackPanel>

                </customUI:SettingsCardV3>

                <customUI:SettingsCardV2 x:Uid="HardenedUpdateProcedureSettingsCard"
                                         IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                                         HeaderIcon="{ui:FontIcon Glyph=&#xF552;}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.UseHardenedInstallationProcess, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                                  x:Uid="ToggleSwitchGeneral"/>
                </customUI:SettingsCardV2>

                <customUI:SettingsCardV3 x:Uid="InstallAppPackagesSettingsCard"
                                         HeaderIcon="{ui:FontIcon Glyph=&#xE83B;}"
                                         IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}">
                    <Button x:Uid="InstallAppPackagesButton"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="{x:Bind ViewModel.InstallButton_Click}"
                            IsEnabled="{x:Bind ViewModel.ElementsAreEnabled, Mode=OneWay}"
                            HorizontalAlignment="Left"/>
                </customUI:SettingsCardV3>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
